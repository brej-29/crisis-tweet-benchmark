# Protocol A (docs/PLAN.md 1.3): mirrors the audited original project
# exactly -- PROJECT_AUDIT.md (sibling repo, section B) confirms
# TfidfVectorizer() and MultinomialNB() were both used with ALL sklearn
# defaults, no arguments passed, no tuning. `max_features: null` = no cap
# (sklearn's actual default), not our project's own 10000 default.
alpha: 1.0
max_features: null
