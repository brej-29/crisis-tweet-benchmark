# Protocol A (docs/PLAN.md 1.3): mirrors the audited original project's
# model_6 (PROJECT_AUDIT.md section B): frozen USE (512-dim) -> Dense(64,
# relu) -> Dense(1, sigmoid), no dropout, epochs=5, Adam defaults
# (lr=0.001), no early stopping. `use_cache_dir` is injected by
# scripts/run_matrix.py, not pinned here.
hidden_size: 64
dropout: 0.0
lr: 0.001
max_epochs: 5
patience: null
restore_best_weights: false
