# DECISIONS.md

One-line-rationale log of every deviation from `docs/PLAN.md`, plus other
non-obvious choices made during implementation. Newest entries at the
bottom. Format: `YYYY-MM-DD — decision — rationale`.

- 2026-07-03 — Repo created at `D:\Project\disaster-tweet-reeval`, separate from the audited course-derived repo (`disaster-tweets-nlp-model-benchmarks`) — plan requires a from-scratch, non-derivative codebase; keeping them as sibling directories makes the separation physically obvious and avoids any accidental import/copy.
- 2026-07-03 — Used `uv init --name dtc --python 3.11 --no-readme`, then deleted the generated `main.py` and hand-configured `pyproject.toml` for a `src/`-layout package (`hatchling`, `packages = ["src/dtc"]`) — `uv init`'s default is a non-package "app" layout; the plan calls for an installable `src/dtc/` package with submodules `data/`, `models/`, `train/`, `eval/`, `analysis/`, `harness/`, so the default scaffold was replaced rather than extended.
- 2026-07-03 — Core deps pinned as ranges in `pyproject.toml` (`pandas>=2.2`, `scikit-learn>=1.5`, `pyyaml>=6.0`, `numpy>=1.26`), exact versions captured in the committed `uv.lock` — ranges express intent/compatibility, the lockfile is the actual reproducibility guarantee per the plan's "uv with committed lockfile" instruction. Dev-only tools (`pytest`, `ruff`) placed in a `dependency-groups.dev` group, not shipped with the installable package.
- 2026-07-03 — `.gitignore` uses explicit per-subdirectory negation patterns for `data/kaggle/` and `data/crisislex/` (rather than a single generic `data/**` wildcard) to keep `manifest.json` and `dropped_conflicting.csv` committed while excluding all raw/split CSVs and the source zip archives — matches the plan's "data/ gitignored except small metadata/hash files" instruction; generic double-star negation patterns are fragile in git and were avoided in favor of explicit, verifiable rules.
- 2026-07-03 — CrisisLex T6 source file supplied by the user directly (`D:\Project\CrisisLexT6-v1.0.zip`, confirmed present, 3,146,913 bytes) rather than fetched programmatically — the user provided the path in this session; Hard Rule 4's "verify text availability" step was therefore done by inspecting the actual local zip contents, not external research. Findings: all 6 event CSVs contain a `tweet` text column (not just IDs) with clean `{on-topic, off-topic}` labels once parsed with a proper CSV reader (quoted fields contain embedded commas/newlines that break naive line-splitting) — see `docs/DATASETS.md` for full detail and the unresolved license-ambiguity note.
- 2026-07-03 — Kaggle raw `train.csv` obtained by extracting it from the already-audited course-derived repo's committed `nlp_getting_started.zip` (`D:\Project\disaster-tweets-nlp-model-benchmarks\nlp_getting_started.zip`) rather than re-downloading from Kaggle — this is the same publicly-distributed competition data file (not code), the plan explicitly forbids programmatic Kaggle download, and the user is the owner of both repos. Written to `data/kaggle/raw/train.csv` (gitignored); raw-file SHA256 = `61111c6dc31eaffa34d1e1fa62e2395325c9bc3b38bba1941a5f1ed9b3fa60df`, recorded in the pipeline manifest, not copied by hand into any code.
- 2026-07-03 — CSV writes/reads for split files and hashing use explicit `lineterminator="\n"` (not the OS default) so that SHA256 hashes recorded in `manifest.json` are reproducible across OSes (local dev is Windows, CI runs on `ubuntu-latest`) — without this, CRLF vs LF line endings would make otherwise-identical splits hash differently between environments.
- 2026-07-03 — Extracted the dedup/split/hash/manifest-fragment logic shared by Kaggle and CrisisLex into `src/dtc/data/common.py`, refactoring `dtc.data.kaggle` to import from it instead of duplicating (done immediately after the Kaggle pipeline was committed, before writing CrisisLex) — the plan's own language ("Same dedup + stratified 80/10/10 + frozen-test discipline") describes one discipline applied to two datasets; keeping it as one code path makes that claim literally true rather than "true if the two copies don't drift." `dtc.data.kaggle`'s public API/behavior is unchanged (re-exports the same names), so already-committed Kaggle tests and manifest continued to pass without modification.
- 2026-07-03 — CrisisLex's dedup+split is run on the 6 events **combined** (not per-event), with an `event` column preserved through every split for later cross-event analysis (per plan 0.3/Task 4) — dedup must see the full combined corpus because generic/off-topic tweet text can plausibly repeat both within and across events, and checking only within one event at a time would miss cross-event leakage between splits.
- 2026-07-03 — CrisisLex `tweet id` column values are stored in the source CSVs wrapped in literal single quotes as part of the cell content (e.g. `"'325208201740029952'"`), discovered by inspecting actual parsed output, not assumed from the README — stripped in `dtc.data.crisislex.load_event_csv` via `.str.strip("'")`.
- 2026-07-03 — Measured CrisisLex dedup numbers (4,705 exact-duplicate rows; 101 conflicting-label groups / 533 rows dropped) are far higher than Kaggle's (110 / 18 groups, 55 rows) — reported as-is in `docs/DATASETS.md` with a hedge on the likely mechanism (recurring generic/off-topic tweets across the geo+keyword sampling), not asserted as confirmed, per the hard rule against fabricating explanations.
- 2026-07-03 — Both datasets' licenses are marked `[UNCLEAR — needs clarification]` in `docs/DATASETS.md` rather than assumed permissive — the Kaggle raw file was obtained locally rather than from the competition rules page, and CrisisLexT6's distributed `README.md` states no explicit license, only a citation request and contact emails. Must be resolved before any public release of derived data or the paper; flagged, not silently assumed away.
- 2026-07-03 — Phase 1: `is_git_dirty`/`build_run_record` upgraded to derive the boolean `git_dirty` from a new `git_dirty_paths` list (`dtc.harness.ledger.get_git_dirty_paths`), excluding only `results/ledger.jsonl` — replaces the old ad hoc boolean so the "ledger dirties itself mid-invocation" behavior documented in `PHASE0_REPORT.md` §5 no longer makes `git_dirty` misleading; the raw path list is also kept in the ledger record for full auditability, not just the derived flag.
- 2026-07-03 — Phase 1: URLs and @mentions are KEPT as tokens in the uniform cleaning protocol (`dtc.data.text.clean_text`), not stripped — disaster tweets can carry signal in both (news links, official-account mentions); this is a documented, ablatable choice per `docs/PLAN.md` 1.1, not a silent default. See `docs/PROTOCOL.md` §1.
- 2026-07-03 — Phase 1: sequence length for token-based models uses the 95th percentile of TRAIN-split token counts (not the mean, which the original audited project used and which cut ~half of all tweets). Measured value on the Kaggle train split (n=5,988, post-cleaning, whitespace tokens): 95th pct = 24 (mean 14.93, median 15, max 31). DistilBERT computes the same percentile policy in its own WordPiece token units rather than reusing 24, since whitespace and WordPiece tokenization produce different counts for the same text. See `docs/PROTOCOL.md` §2.
- 2026-07-03 — Phase 1: `scripts/precompute_use.py` reads raw text via its own CSV-loading path, not `dtc.eval.frozen_test_loader`, even when embedding val/test splits — the frozen-test GUARD governs who may read test *labels* for evaluation/selection purposes; embedding precompute reads only the `text` column of every split (train/val/test, all three, for both datasets) to build a reusable feature cache, and never reads or returns the label column. This is intentionally outside the guard's scope (it isn't a training/selection code path reading labels), but is called out explicitly here so the nuance isn't mistaken for a guard bypass.
- 2026-07-03 — Phase 1 Protocol A (E2) hyperparameters were pulled from `PROJECT_AUDIT.md` (sibling repo `disaster-tweets-nlp-model-benchmarks`, consulted fresh this session, not copied from earlier context) rather than guessed, per the task's "sensible defaults mirroring the audited project where they exist" instruction: `Embedding(10000, 128)` for every from-scratch model, `LSTM(64)`/`GRU(64)`/`Bidirectional(LSTM(64))`/`Conv1D(filters=32, kernel_size=5)` architectures, no dropout anywhere, `epochs=5`, Adam defaults (lr=0.001), no early stopping, and — notably — `TfidfVectorizer()`/`MultinomialNB()` with ALL sklearn defaults (no `max_features` cap at all, unlike this project's own 10000 default). The audit also revealed the original notebook's markdown claims a "Logistic Regression" model that was never actually executed (only MultinomialNB was); `tfidf_logreg` and `distilbert_finetune` therefore have NO original equivalent to mirror and use documented plain defaults instead, noted as such in their `configs/protocol_a/*.yaml` files. USE's original architecture (`Dense(64, relu) -> Dense(1, sigmoid)`, frozen encoder, no dropout) matches this project's own `use_frozen` default `hidden_size=64` already, confirmed rather than assumed.
- 2026-07-03 — Phase 1: `scripts/precompute_use.py` gained an `--extra-csv` option (and `main(..., extra_csv_paths=...)`) so Protocol A's `use_frozen` runs have full USE-embedding cache coverage over the RAW (non-deduped) Kaggle csv, not just the Protocol B prepared splits — the ~165 rows Protocol B's dedup policy drops would otherwise raise `KeyError` in `UseFrozenModel` if they land in Protocol A's 90/10 split. Cache entries are keyed by text SHA256 regardless of source, so this is purely additive to the existing per-dataset cache, not a second cache.
- 2026-07-03 — Introduced a `stage` field on every ledger run record (values used so far: `"tuning"`, `"E1"`, `"E2"`, `"E3"`, or `None`) rather than adding a separate `experiment` field alongside it — one field fully identifies "what this run is for" without redundant/potentially-inconsistent dual bookkeeping. `docs/PLAN.md`'s "ledgered with stage: tuning" language is satisfied literally, and scripts/run_matrix.py reuses the same field to tag E1/E2/E3 runs and to compute its skip-key.
- 2026-07-03 — `dtc.models.torch_common.fit_with_early_stopping[_generic]` gained `patience: int | None` (None disables early stopping) and `restore_best_weights: bool` (False keeps final-epoch weights instead of the best-val-loss checkpoint) — needed so Protocol A's "no early stopping, fixed N epochs" replication (docs/PLAN.md 1.3) could reuse the exact same model classes and training loop as Protocol B, rather than forking a separate no-early-stopping implementation per model.
- 2026-07-04 — Bug found during real smoke verification (Task 8): `scripts/precompute_use.py` was caching USE embeddings keyed by SHA256 of the RAW (uncleaned) CSV text, while `dtc.models.use_frozen` hashes CLEANED text (`dtc.data.text.clean_text`) at fit/predict time per docs/PROTOCOL.md §4 ("USE consumes cleaned raw strings") — a real KeyError on the first live `run_matrix.py --smoke --only tuning` invocation, not a hypothetical. Fixed by having `precompute_use.py` clean text before hashing/embedding, matching the consumer. The stale (wrongly-keyed) `data/kaggle/use_embeddings/` cache was deleted and regenerated rather than left alongside the corrected files.
- 2026-07-04 — The crash above happened after 40 of 52 tuning-smoke runs had already succeeded and been ledgered (the crash was inside `use_frozen`'s `fit`, the 8th of 9 models in enumeration order); re-running `run_matrix.py --smoke --only tuning` after the fix correctly skipped those 40 and ran only the remaining 12 (`use_frozen` + `distilbert_finetune`) — an unplanned but genuine real-world demonstration of Task 6's resumability guarantee (a killed/crashed run leaves no partial ledger line and doesn't force redoing already-ledgered work), not just the synthetic fixture in `tests/test_run_matrix.py`.
- 2026-07-04 — Bug found while wiring up `configs/final/README.md` (Task 8): `scripts/select_configs.py`'s docstring claimed smoke runs are ignored, but `select_best_configs` only filtered on `stage == "tuning"`, with no smoke check — so once real (non-smoke) tuning runs eventually share the ledger with smoke tuning runs, a smoke config could still outrank/contaminate the real selection. Fixed by adding an `include_smoke` parameter (default `False`, matching `make_tables.py`'s `--include-smoke` pattern) that must be passed explicitly to consider smoke-tagged tuning records; the Task 8 demonstration re-ran `select_configs.py --include-smoke` deliberately, since real tuning hasn't happened yet.
- 2026-07-04 — `scripts/make_tables.py`'s T1 builder now refuses (`MixedConfigError`) if any model has more than one distinct `config_id` among its non-smoke E1 records, naming the model and the conflicting config_ids — e.g. if `configs/final/lstm.yaml` is edited and E1 is re-run without clearing the old ledgered runs, pooling old- and new-config runs into one mean/std would silently misreport variance as "seed variance" when part of it is actually config drift. Smoke E1 records are exempt from this check (they may legitimately use different placeholder configs across a session), matching the same smoke-exemption pattern as the dirty-source-path check.
- 2026-07-06 — Phase 1.5 Task 1: extended the mixed-config guard to T4 (`check_single_config_per_model_fraction`, grouped by `(model_name, train_fraction)` instead of `model_name` alone — E3 legitimately varies the model config's effective behavior across fractions only via the fraction itself, not the config, so two different config_ids at the SAME fraction for the SAME model is the real signal of drift). T2/T3 were deliberately left without their own explicit guard call: T1 is evaluated first in `main()`'s table dict (Python evaluates dict-literal values left to right), so a `MixedConfigError` in T1 aborts table generation before T2/T3/T4 ever run — they "inherit" the guard by execution order, not by duplicating the check.
- 2026-07-06 — Phase 1.5 Task 1: chose the `--only-config-ids-from <dir>` recovery mechanism over a manual `--config-id-allowlist model=config_id` flag. Rationale: `configs/final/<model>.yaml` is already this project's single source of truth for "what config should E1/E3 use" (populated by `scripts/select_configs.py`), so resolving each model's CURRENT config_id directly from that directory needs no new bookkeeping and can't drift from `configs/final/` the way a hand-maintained allowlist could. The one wrinkle: `scripts/run_matrix.py`'s `resolve_config()` injects `use_cache_dir` into `use_frozen`'s config before computing its real `config_id`, so a bare re-hash of `configs/final/use_frozen.yaml` would never match a real ledger entry. `resolve_final_config_ids()` replicates just that one injection locally in `make_tables.py` rather than importing `run_matrix.py` (Phase 1.5 Hard Rule 1 forbids touching the driver this session) — for the other 8 models, config resolution for E1/E3 is the bare YAML file, so no injection is needed and the hashes match exactly.
- 2026-07-06 — Phase 1.5 Task 2: audited `docs/COLAB.md` against the fact that `data/` is gitignored and found it incomplete — it jumped straight from "install deps" to "run the driver," silently assuming raw CSVs, prepared splits, and the USE cache already existed on the Colab runtime, none of which a fresh clone has. Patched to add: uploading the two raw files to their exact expected paths (and discovered `dtc.data.crisislex.load_and_combine_events` expects the 6 event CSVs FLAT in `data/crisislex/raw/`, not nested one level deeper as the distributed zip has them — the runbook now flattens on extraction); a manifest-identity check (`git status --porcelain data/` must be empty after regenerating splits on Colab) with an explicit STOP instruction if it isn't; running `precompute_use.py` before any `use_frozen` run; a clean-tree requirement before training; and a two-round Colab sequencing (tuning -> local merge/select/commit/push -> E1/E2/E3) so E1/E3 never train against a stale/placeholder `configs/final/` — stated inline rather than pointing at `EXECUTION_RUNBOOK.md`, which doesn't exist in this repo. Also updated the clone URL from a `<you>/disaster-tweet-reeval` placeholder to the real remote, `https://github.com/brej-29/crisis-tweet-benchmark`.
- 2026-07-06 — Pushed the repo to `https://github.com/brej-29/crisis-tweet-benchmark` (branch `master`) at the user's explicit request. The local project folder was NOT renamed to match (`crisis-tweet-benchmark`) this session — the harness pins this session's Bash/PowerShell working directory to the original absolute path and resets it before every command (confirmed empirically: a `cd` in one command did not persist to the next), so renaming the active folder would have broken shell-tool access for the rest of the session. Deferred to the user to rename between sessions instead.
- 2026-07-06 — Phase 1.6: pinned `torch` to the CUDA build `2.12.1+cu126` on Windows only, via `[[tool.uv.index]]` (`explicit = true`, `https://download.pytorch.org/whl/cu126`) + `[tool.uv.sources]` gated on `marker = "sys_platform == 'win32'"` in `pyproject.toml`, rather than an ad hoc `pip install`. Rationale: the full Phase 1 matrix runs locally on this machine's RTX 3050 (Ampere/sm_86), and an ad hoc install would be silently reverted by the next `uv sync`; a lockfile-driven index pin survives it. `cu126` (CUDA 12.6) was chosen over the newer `cu130`/`cu132` variants that also exist for `torch==2.12.1` on Windows because the task specifically called for the newest **cu12x** line (CUDA 12.6 is in fact the only 12.x line still published for this torch version on win_amd64 — the other two are already CUDA 13.x) and is comfortably within the installed driver's supported range (`nvidia-smi` reports max CUDA 13.0). The Windows-only marker leaves CI's Linux resolution (`ubuntu-latest`, plain `uv sync`) on the ordinary PyPI `torch==2.12.1` wheel, confirmed unchanged by the `uv.lock` diff. Verified: `torch.cuda.is_available()` is `True`, device `NVIDIA GeForce RTX 3050 6GB Laptop GPU` (capability 8.6); one real `distilbert_finetune` E1 smoke run (seed 2) via `scripts/run_matrix.py --smoke --only e1 --models distilbert_finetune --seeds 2` completed in 42s on GPU (vs. the 2-6 min CPU smoke estimate in `PHASE1_REPORT.md` §6), confirmed on-GPU by the CUDA-only `attention_backward.cu` determinism warning it raised. Side effect, not caused by this change but surfaced by it: a bare `uv sync` (matching CI's invocation exactly) uninstalled the `tf` optional-dependency group (`tensorflow`/`tensorflow-hub`), which had been present in the local venv from an earlier out-of-band `--extra tf` sync; `tests/test_precompute_use.py` skips gracefully via `pytest.importorskip("tensorflow_hub")` rather than failing, so the full suite is still green (135 passed, 1 skipped) — anyone running `scripts/precompute_use.py` locally will need `uv sync --extra tf` again.
- 2026-07-16 — Phase 2 Task A1: old (Phase 1) ledger records get `train_dataset`/`eval_dataset` backfilled at READ time only (`dtc.harness.ledger.read_ledger`, default = the record's `dataset`, else "kaggle"), never written back to the file — the ledger is append-only and every Phase-1 run both trained and evaluated on kaggle, so read-time defaulting is lossless and keeps history byte-identical.
- 2026-07-16 — Phase 2 Task A1: if a dual-eval (E4/E5) training crashes between its two frozen-test evals, resume retrains from scratch and fills ONLY the missing eval record(s) — the two records then carry different `training_id`s, which is acceptable because training_id is provenance-only ("these records came from one in-memory model"), never a correctness key; the skip-key that governs pending/skip is per eval record and doesn't involve training_id.
- 2026-07-16 — Phase 2 Task A1: `UseFrozenModel` gets cross-dataset embedding-cache access via a public post-fit attribute `extra_cache_dirs` set by the driver, deliberately NOT via `config` — config participates in `config_id`, and E5's use_frozen must keep a config_id identical to E1's (same tuned config, same train dataset/cache) so T7's cross-dataset comparisons line up; predict-time lookups search the primary (train-dataset) cache first, then each extra dir (`dtc.data.use_cache.load_embeddings_multi`).
- 2026-07-16 — Phase 2 Task A2: CrisisLex's `event` column (needed later for per-event table T6) is threaded through as a generic `extra_columns: dict[str, Sequence] | None` param on `save_predictions`/`log_evaluation_run`, not a dataset-specific `event` kwarg — `evaluate_model_on_frozen_test` emits it only when `load_frozen_test_standardized` actually returned an `event` column (crisislex), so kaggle's `eval_fields` dict has no `extra_columns` key at all and its predictions.csv shape is byte-identical to before this task. `scripts/run_matrix.py`'s existing `**eval_fields` unpacking at the dual-eval call site already routes the key into `log_evaluation_run` with no driver-side special-casing needed — confirmed by `tests/test_run_matrix.py::test_dual_eval_predictions_csv_carries_event_only_for_crisislex`, which runs the real driver end-to-end and checks the crisislex eval record's predictions.csv has `event` while the kaggle eval record (same training) doesn't.
- 2026-07-16 — Phase 2 Task A2: confirmed (not just assumed) that `dtc.eval.frozen_test_loader`'s runtime guard and `tests/test_frozen_test_guard.py`'s static source scan were already fully dataset-generic — the runtime guard only inspects the caller's module/file, never the CSV path, and the static scan only greps for the literal string `frozen_test_loader` in guarded-subtree files, never a dataset name. Rather than leave that as an unverified assumption, added explicit crisislex-path test cases mirroring the existing kaggle ones, plus `test_static_scan_is_dataset_generic_not_kaggle_only`, which builds synthetic kaggle/crisislex/third-dataset offender files under a `tmp_path` guarded subtree and asserts the (now factored-out) `_scan_for_frozen_test_references` helper flags all three identically.
- 2026-07-16 — Phase 2 Task A4: added an optional per-experiment `phase` key to `configs/experiments.yaml` (`scripts/run_matrix.py`'s `build_run_specs` reads `exp.get("phase", "phase1")` into each spec, and `execute_run` passes `spec["phase"]` instead of the old hardcoded literal `"phase1"`) — `dtc.harness.run.log_evaluation_run` already had a `phase` param going back to Phase 0, but the driver never varied it; E4/E5 need their ledger records tagged `phase2` while every existing Phase 1 experiment (tuning/e1/e2/e3, no `phase` key) keeps writing `phase1` exactly as before, so the default preserves all existing behavior with zero changes to Phase 1 config files.
- 2026-07-16 — Phase 2 Task A4: E4/E5 reuse Kaggle-tuned `configs/final/*.yaml` verbatim (no CrisisLex tuning), so no config file gained a CrisisLex-specific `max_length` — the measured 95th-percentile values (whitespace and DistilBERT WordPiece, CrisisLex train split) are recorded in `docs/PROTOCOL.md`'s new "CrisisLex (Phase 2)" subsection purely for reference/audit, matching how Kaggle's own whitespace p95 is documented but not itself a pinned config value (only `configs/final/{lstm,gru,bilstm,conv1d,meanpool_embed}.yaml`'s Kaggle-measured `max_length: 24` is actually used at fit time for E4, since those configs are reused as-is).
- 2026-07-16 — Phase 2 Task A4: Kaggle's DistilBERT WordPiece 95th-percentile was never previously recorded anywhere in the repo (confirmed by reading an E1 `distilbert_finetune` ledger record's `config` snapshot: only `dropout`/`lr`, no `max_length` — it's computed at fit time, not ledgered) — measured it now the same way (train-only, `distilbert-base-uncased` tokenizer) for completeness alongside the new CrisisLex numbers, clearly labeled measured-now rather than implying it was captured live during E1.
- 2026-07-16 — Phase 2 Task A5: T5/T6's mixed-config guard groups by `(model, train_dataset)` rather than `(model, train_dataset, eval_dataset)` or per model alone — a training's config_id is fixed once (E4/E5 train one in-memory model, then evaluate it on both frozen tests), so config drift can only show up ACROSS trainings sharing the same train_dataset, never between a training's own two eval records; grouping any finer would just re-derive the same partition with extra bookkeeping, and grouping by model alone would wrongly conflate E4 (crisislex-trained) and E5 (kaggle-trained) runs of the same model, which legitimately use different use_frozen config_ids. T7's guard groups by `(model, stage)` instead, checking E1 and E5 for internal (same-stage) drift separately rather than requiring E1's config_id to equal E5's — T7's whole point is comparing E1 and E5 AS RUNS (the reproducibility check itself), so requiring their config_ids to match would make the check tautological for any model whose E5 config differs from E1 by construction.
- 2026-07-16 — Phase 2 Task A5: `resolve_final_config_ids_for_datasets`/`filter_to_final_config_ids_by_train_dataset` (new, alongside the existing single-dataset `resolve_final_config_ids`/`filter_to_final_config_ids`, which T1-T4 keep using unchanged) resolve use_frozen's expected config_id once per `train_dataset` actually present in the ledger, keyed `(model_name, train_dataset)` — `use_frozen`'s config_id depends on which dataset's `use_cache_dir` gets injected (Phase 1.5's one injection, replicated per dataset here), so E4's crisislex-trained and E5's kaggle-trained `use_frozen` records genuinely resolve to different expected config_ids from the SAME `configs/final/use_frozen.yaml` file. A single `dataset="kaggle"` resolution (T1-T4's need) would never match E4's records, so `--only-config-ids-from` would have silently dropped all of them; `main()` now derives its `datasets_present` from the records actually being tabled (still just `{"kaggle"}` for T1-T4, so their output stays byte-identical) instead of hardcoding "kaggle".
